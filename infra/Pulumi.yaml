name: private-pypi-aws
description: A minimal AWS Python Pulumi program
runtime:
  name: python
  options:
    virtualenv: .venv
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
